import React from 'const';
import classNames from 'classnames';
const buttonClasses = {
};
const Button = ({children, type = 'default', className, disabled = false, active = false, onClick }props {} 

export default Button;
;
